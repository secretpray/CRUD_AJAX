// if(document.getElementById('user_<%= @user.id %>')) {
//   document.getElementById('user_<%= @user.id %>').remove()
// }

var tableListSection = {}
tableListSection = document.querySelector('#table-body')

tableListSection.innerHTML = "<%== j render 'table', users: @users %>"

// create new pagination (TODO remake with JS)
var pagySection = {}
pagySection = document.querySelector('.pagination-link')
pagySection.innerHTML = "<%== j render partial: 'pagy/bootstrap_nav', locals: { pagy: @pagy } %>"
// fix bug in create link
document.querySelectorAll('.page-link').forEach((link) => { link.href = link.href.replace(/\/<%= @user.id %>/gi, '') } )
